/*
  MUTATIONS - synch methods for changing state;
  BUILD:
     mutations: {
      mutationMethod(state, payload) {
        ... changing state ...
      }
    }
  USAGE INSIDE COMPONENTS (BAD PRACTISE because sych): store.commit('mutationMethod', { value })
 */
export default {
}